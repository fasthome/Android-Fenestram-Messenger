<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    app:layout_alignSelf="flex_start"
    app:layout_flexGrow="1.0">

    <ImageView
        android:id="@+id/recent_image_item"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginHorizontal="5dp"
        android:layout_marginVertical="10dp"
        android:minWidth="100dp"
        android:minHeight="100dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:minHeight="100dp"
        tools:minWidth="100dp" />

    <TextView
        android:id="@+id/recent_image_show_all"
        style="@style/ProfileGuestMoreImages"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@drawable/shape_alpha_background"
        android:gravity="center"
        android:text="@string/more_images"
        app:layout_constraintBottom_toBottomOf="@+id/recent_image_item"
        app:layout_constraintDimensionRatio="1:1"
        app:layout_constraintEnd_toEndOf="@+id/recent_image_item"
        app:layout_constraintStart_toStartOf="@+id/recent_image_item"
        app:layout_constraintTop_toTopOf="@+id/recent_image_item" />

</androidx.constraintlayout.widget.ConstraintLayout>